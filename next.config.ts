import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['smartbtc.io', '*.smartbtc.io', 'bscscan.com', '*.bscscan.com', 'pancakeswap.finance', 'raw.githubusercontent.com'],
  },
};

export default nextConfig;
